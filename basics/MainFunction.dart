/**
 * Learning Dart - Main Function.
 * - Every app written in Dart needs a method called 'main'.
 *  Its how the Dart knows where to start the application.
 *  Every executable app needs a main method, except libraries.
 *  It can run without arguments, but also can accept cli arguments with:
 *  List<String> args.
 */

void main() {
  print('Hello from Dart');
}
